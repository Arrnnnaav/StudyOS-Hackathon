import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function PairingGuidePage() {
  return <main className="mx-auto max-w-3xl space-y-6"><header><p className="text-sm font-semibold text-emerald-700">POINT & ASK</p><h2 className="mt-1 text-3xl font-bold">Connect the Chrome extension</h2><p className="mt-2 text-slate-500">Pair the LearningHQ Chrome Web Store extension to your account with a one-time code. The code expires in ten minutes.</p></header><Card><CardHeader><CardTitle>Pair in four steps</CardTitle></CardHeader><CardContent><ol className="list-decimal space-y-3 pl-5 text-slate-700 dark:text-slate-300"><li>Install the LearningHQ Point &amp; Ask extension from the Chrome Web Store link provided by your institution.</li><li>Open any lesson or study page, select text, then choose the LearningHQ extension.</li><li>Return to Settings and select Get pairing code. Copy the six-character code.</li><li>Choose Pair with LearningHQ in the extension, enter the code, and select Connect.</li></ol></CardContent></Card><Card><CardHeader><CardTitle>Permissions and troubleshooting</CardTitle></CardHeader><CardContent className="space-y-3 text-sm text-slate-600 dark:text-slate-300"><p>The extension reads only the page you explicitly invoke it on and sends only your marked text with your question. It never receives a password.</p><p>If the code expires or is already used, generate a new code in Settings. The published extension connects securely to LearningHQ automatically.</p><p>To disconnect, remove the extension from Chrome and pair again with a new code when needed.</p><Button asChild variant="outline"><Link href="/dashboard/settings">Back to Settings</Link></Button></CardContent></Card></main>
}
