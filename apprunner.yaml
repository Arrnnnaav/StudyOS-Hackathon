version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - corepack enable
      - corepack prepare pnpm@12.4.2 --activate
      - pnpm install --frozen-lockfile
    build:
      - pnpm build
run:
  command: pnpm start
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
